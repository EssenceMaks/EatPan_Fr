# Стандарт Keep a Changelog

Усі помітні зміни в проєкті EatPan документуються тут.
Формат базується на [Keep a Changelog](https://keepachangelog.com/uk/1.1.0/).

---

## [0.5.0] - 2026-03-28

### Added

- **Vintage Kanban Board** — реалізовано повноцінний модуль "Craft Space" для управління задачами з 4-ма колонками: GET, DO, VERIFY, DONE.
- **Інтерактивний MD-Сайдбар** — панель задач `tasks.md` перетворена на інтерактивний список:
  - Додано глобальні вінтажні перемикачі (іконки `eye`/`eye-off` + A/KB) в заголовок для керування видимістю архівів та бейджів.
  - При наведенні (`hover`) на задачі тепер з'являється панель дій (Архівувати або Створити картку).
- **Синхронізація Сайдбару та Kanban** — нативний браузерний алерт створення картки замінено на **Власне Slide-In Модальне Вікно**, що плавно виїжджає знизу екрану. Після підтвердження задача назавжди пов'язується з новоствореною Kanban-карткою через `localStorage`.
- **Авто-Навігація Картками** — натискання на іконку пов'язаної задачі в сайдбарі активує `scrollIntoView` зі стильною пульсуючою CSS-анімацією (`flash-highlight`) на цільовій картці.
- **Smart Scroll** — додана "розумна" прокрутка для Kanban-дошки. Над заповненими колонками працює вертикальний скрол, у вільному просторі — перемикається на горизонтальний скрол усієї дошки.
- **HTML5 Drag-and-Drop** — додано механіку перетягування карток мишею між колонками. Включає підсвічування drop-зони та збереження стану в `localStorage`.
- **Сенсорна (Touch) підтримка** — переміщення карток на мобільних пристроях здійснюється миттєвими натисканнями по кнопкам-статусам, організованим у зручну сітку 2x2 при розкритті задачі.

### Changed

- **Структура логів** — головний \`CHANGELOG.md\` повернено у кореневу директорію проєкту за запитом, оновлено воркфлоу агента.

### Fixed

- **Баг рендерингу SPA (Single Page Application)** — виправлено критичну помилку, коли натискання кнопок зміни статусу візуально ігнорувалися. Проблема виникала через те, що `main.js` (`initInfiniteScroll`) дублює DOM-вузли для каруселі блоків навігації, створюючи клони ідентифікаторів (`id`). Пошук елементів переведено з `document.getElementById` на `document.querySelectorAll` для синхронного оновлення всіх копій UI.
- **Баг Drag-and-Drop "Ghost Image"** — виправлено скасування події браузером (через обов'язковий `dataTransfer.setData`) та налаштовано асинхронне встановлення прозорості за допомогою `setTimeout`, що зберігає видимим "привид" картки під час її перетягування.

### Changed

- **UI Hover-Ефекти** — миттєву зміну `display: none` замінено на плавні 0.5s CSS-анімації (`max-height`, `opacity`, `transform`) для розкриття детальної інформації про завдання.
- **Kanban-адаптивність** — встановлено ліміти ширини `max-width: 85vw` на загальний контейнер `.section_block` та адаптивний `flex: 1` для внутрішнього контейнеру, що усунуло баг безкінечного розтягування екрану.
- **Структура логів** — головний \`CHANGELOG.md\` повернено у кореневу директорію проєкту за запитом, оновлено воркфлоу агента.
- **Інтерфейс Архіву** — заархівовані задачі тепер не викреслюються, а лише стають напівпрозорими (сірими), якщо вони не відмічені як виконані (checkbox клікабельний через клієнтське відстеження `completedTasks`).

### Removed

- Видалено тестові файли UIKit, експорти Figma та попередні логи Figma (`figma_changelog.md`, `figma_validation.md`), оскільки вони більше не релевантні після ручної доробки коду.

---

## [0.4.0] - 2026-03-27

### Added

- **Інтеграція Figma MCP Server** — виправлено конфігурацію (`npx.cmd`) для обходу Execution Policy у Windows, що дозволило підключити інструменти Figma до агента Antigravity.
- **Правила синхронізації з Figma** — створено файл `.agents/rules/sync_with_figma.md`, що описує строгий двосторонній процес перенесення дизайну між макетом та кодом.
- **Воркфлоу вирішення конфліктів** — додано крок валідації: тепер агент фіксує всі розбіжності UI у файлі `docs/changelogs/figma_validation.md` та запитує дозвіл розробника перед перезаписом локального коду.
- **Історичний лог Figma** — створено файл `docs/changelogs/figma_changelog.md` для збереження історії застосованих змін дизайну (Figma -> Код або Код -> Figma).
- **Контекст ініціалізації** — додано файл `.agents/rules/context_mcp_restart.md` для фіксації відправної точки роботи з макетом.

### Changed

- **Структура логів** — головний `CHANGELOG.md` та файли логування переміщено до виділеної папки `docs/changelogs/`.
- **Оновлення project_rules** — в `.agents/rules/project_rules.md` додано обов'язкову вимогу звірятися з Figma перед внесенням змін у UI.

---

## [0.3.0] - 2026-03-23

### Added

- **Анімація гортання сторінок** — при переході в режим створення рецепту та назад обидві сторінки книги виконують 3D flip-анімацію (rotateY через spine), імітуючи гортання сторінок. CSS keyframes `flip-left` / `flip-right` з `perspective(1200px)`.
- **Вибір іконки для нової категорії** — при створенні користувацької категорії відкривається picker з 29 Lucide іконками їжі та напоїв (apple, banana, beef, beer, cake-slice, candy, carrot, cherry, coffee, cookie, croissant, cup-soda, drumstick, egg-fried, fish, grape, ham, ice-cream-cone, leaf, milk, nut, pizza, salad, sandwich, shrimp, soup, utensils, wheat, wine).
- **Тонкий скролбар в режимі створення** — видимий 4px скролбар на overlay-формах. На правій сторінці (форма рецепту) скролбар зліва (біля spine), на лівій (категорії) — справа (біля spine). Реалізовано через `direction: rtl` трік.

### Changed

- **Bookmark кнопка динамічна** — в режимі створення іконка змінюється з `feather` на `arrow-left`, текст з «Створити рецепт» на «До рецептів». При поверненні — відновлюється.
- **Мульти-вибір категорій** — `selectCategory()` тепер toggle (можна обрати кілька категорій одночасно, повторне натискання знімає вибір).
- **Нова категорія з іконкою** — замість простого текстового поля тепер є preview-кнопка обраної іконки + picker + поле назви + кнопка додавання. Нова категорія додається в сітку з обраною іконкою і автоматично виділяється.

### Documentation

- Оновлено `CHANGELOG.md` — додано запис [0.3.0].
- Оновлено `docs/features/recipe-creation.md` — додано опис анімації, icon picker, мульти-вибору.

**Назва коміту:**
`feat: page flip animation, multi-select categories, icon picker, scrollbar UX`

---

## [0.2.0] - 2026-03-23

### Added

- **Режим створення рецепту** — натискання стрічки «Створити рецепт» перемикає обидві сторінки книги в режим створення (overlay замість модалки). Ліва сторінка: вибір книги (чекбокси) + категорія (сітка іконок). Права сторінка: форма з назвою, описом, фото, інгредієнтами, кроками, секретом шефа, подачею.
- **Динамічні рядки форми** — кнопки «Додати інгредієнт» / «Додати крок» динамічно додають нові рядки в форму створення.
- **Стилі режиму створення** — новий файл `src/modules/recipe-book/styles/create-recipe.css` з повним оформленням overlay-форм у стилістиці книги.
- **Папка документації** — `docs/features/` для опису функціоналу та TODO.
- **Рекомендації з рефакторингу** — `docs/refactoring-plan.md` з порадами щодо розбиття `main.js` та `global.css`.

### Fixed

- **Прокрутка книги на 13" екрані** — додано вертикальну прокрутку секції cookbook (`overflow-y: auto`). Адаптивний `min-height: min(850px, calc(100vh - 200px))` на сторінках книги.
- **Заголовок секції в active-режимі** — h2 та текст секції приховуються коли блок активний, звільняючи місце для контенту.
- **Grid page--right** — `grid-template-rows` змінено на `1fr` щоб єдиний flow-child (scrollable-area) займав весь доступний простір.

### Changed

- **Bookmark onclick** — стрічка «Створити рецепт» тепер викликає `window.toggleCreateRecipe()` замість `window.toggleEdit()` (стара модалка залишена для сумісності).
- **Пір'їнко (feather tab)** — бічна вкладка редагування тепер також викликає `window.toggleCreateRecipe()`.
- **Padding книги** — зменшено `book-module-wrapper` padding з `2rem 1rem` до `1rem` для кращого використання простору.

**Назва коміту:**
`feat: recipe creation mode in-book, fix cookbook scroll on 13-inch screens`

---
