# Створення рецепту (Recipe Creation)

## Опис функціоналу

Замість модального вікна, створення рецепту відбувається **всередині книги** — обидві сторінки перемикаються в режим створення (overlay поверх звичайного контенту).

### Точки входу

| Елемент | Розташування | Дія |
|---------|-------------|-----|
| Стрічка «Створити рецепт» | Закладка зверху книги (bookmark-btn) | `window.toggleCreateRecipe()` — новий рецепт |
| Пір'їнко (feather) | Бічна вкладка справа внизу (side-tabs--right-bottom) | `window.toggleCreateRecipe()` — редагування поточного |

### Ліва сторінка (PageLeft overlay)

- **Заголовок:** «Новий рецепт»
- **Додати до книги** — чекбокси: Всі рецепти, Особисті, Гості, Заклади
- **Категорія** — сітка кнопок з іконками Lucide (Супи, М'ясо, Птиця, Риба, Морепродукти, Паста, Салати, Випічка, Десерти, Напої)
- **Нова категорія** — текстове поле + кнопка «+» для створення власної категорії

### Права сторінка (PageRight overlay)

- **Назва рецепту** — великий текстовий input
- **Короткий опис** — italic input (напр. «Хрусткі та соковиті»)
- **Фото** — область для завантаження зображення (drag/click)
- **Час / Порції** — два коротких input з іконками clock та users
- **Інгредієнти** — динамічний список (назва + кількість + кнопка видалення), кнопка «Додати інгредієнт»
- **Процес приготування** — динамічний список кроків (номер, назва кроку, опис), кнопка «Додати крок»
- **Секрет шефа** — textarea з бордюром зліва
- **Подача** — textarea з рекомендацією до подачі
- **Дії:** «Зберегти в книгу» (brand-red) + «Скасувати»

### Технічна реалізація

- CSS клас `.book--create-mode` на `.book-cover` перемикає видимість overlay
- Overlay (`create-mode-content`) — `position: absolute` поверх сторінки з `z-index: 150`
- Бічні вкладки та нижні стрічки ховаються в create-режимі
- Lucide іконки переініціалізуються після toggle (`lucide.createIcons()`)
- Всі onclick функції зареєстровані на `window.*` (паттерн проєкту)

### Файли

| Файл | Що містить |
|------|-----------|
| `src/modules/recipe-book/components/layout/PageLeft.js` | Overlay лівої сторінки (книги + категорії) |
| `src/modules/recipe-book/components/layout/PageRight.js` | Overlay правої сторінки (форма рецепту) |
| `src/modules/recipe-book/components/layout/Book.js` | Bookmark onclick → `toggleCreateRecipe()` |
| `src/modules/recipe-book/styles/create-recipe.css` | Всі стилі режиму створення |
| `main.js` | `window.toggleCreateRecipe`, `selectCategory`, `addIngredientRow`, `addStepRow`, `saveRecipe` |

---

## TODO — що ще необхідно зробити

### Критичне (MVP)

- [ ] **Збір даних форми** — `saveRecipe()` зараз mock. Потрібно зібрати всі поля форми в об'єкт і передати в `RecipeService.create()`
- [ ] **Валідація полів** — мінімум: назва рецепту обов'язкова, хоча б один інгредієнт, хоча б один крок
- [ ] **Підключення до Django API** — `RecipeService.create()` → `POST /api/v1/recipes/`
- [ ] **Оновлення списку після створення** — після успішного збереження, перезавантажити рецепти в BookModule і перемкнутися на новий рецепт

### Важливе

- [ ] **Режим редагування** — пір'їнко повинно заповнювати форму даними поточного рецепту (зараз відкриває порожню форму)
- [ ] **Завантаження зображення** — підключити file input до preview та upload на бекенд
- [ ] **Нова категорія** — зберігати нові категорії та показувати їх у сітці
- [ ] **Drag & drop кроків** — можливість перетягувати кроки для зміни порядку
- [ ] **Автозбереження чернетки** — зберігати стан форми в localStorage щоб не втратити при випадковому закритті

### Покращення UX

- [ ] **Анімація переходу** — плавне перемикання між view/create режимами (fade або slide)
- [ ] **Toast повідомлення** — «Рецепт збережено!» замість простого закриття
- [ ] **Підтвердження скасування** — якщо форма заповнена частково, питати «Ви впевнені?»
- [ ] **Підрахунок калорій** — автоматичний розрахунок на основі інгредієнтів (після підключення API)
- [ ] **Теги** — додати можливість вказувати теги (#швидко, #дієтичне, #святкове)
